import { Theme } from "../../../../theme"

// tslint:disable-next-line:no-any
export const styles = {
	root: {
		padding: 0,
		color: Theme.primary,
		"& checked": {
			color: Theme.primary
		}
	},
	checked: {}
}
