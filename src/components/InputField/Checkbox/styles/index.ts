import { Theme } from "../../../../theme"

export const styles = {
    root: {
        color: `${Theme.secondary}!important`
    },
    checked: {
        color: `${Theme.secondary}!important`
    }
}
